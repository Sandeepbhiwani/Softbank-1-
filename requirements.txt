# Requirements inferred from the codebase and README
# Django 5.2 was used to generate migrations in the repo; use 5.2.x series
Django>=5.2,<6.0

# Forms and styles
django-crispy-forms>=2.0
crispy-tailwind==1.0.3
# HTTP calls
requests>=2.30.0

# Production / Hosting
gunicorn>=20.1.0
whitenoise>=6.0
psycopg2-binary>=2.9

# Environment and DB helpers
dj-database-url>=1.0.0
django-environ>=0.9.0

# Optional: If you use Django AllAuth & social auth (README mentions it)
django-allauth>=1.8.0

# Optional recommended dev tools
coverage>=6.5.0
pytest>=7.0.0

# If your project evolves to use REST or async APIs, add djangorestframework or channels
# djangorestframework>=3.14.0
# channels>=4.0.0

